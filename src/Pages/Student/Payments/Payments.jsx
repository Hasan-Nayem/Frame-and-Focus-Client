import { Helmet } from "react-helmet";

const Payments = () => {
    return (
        <div>
            <Helmet>
                <title>Student | Payment History</title>
            </Helmet>
        </div>
    );
};

export default Payments;